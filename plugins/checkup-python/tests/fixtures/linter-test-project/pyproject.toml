[project]
name = "linter-test-project"
version = "0.1.0"
description = "A test project with intentional linting issues"
requires-python = ">=3.9"
dependencies = [
    "requests>=2.31.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=7.0.0",
]
